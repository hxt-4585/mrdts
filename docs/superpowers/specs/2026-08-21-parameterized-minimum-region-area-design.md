# 参数化最小区域面积验证设计

## 目标

默认区域地图可以不均衡；测试不再要求各区域面积接近。每个区域只需满足其配置的最小面积比例。

## 设计

- 保持 `config/region.toml` 的 `area_imbalance` 配置不变。
- 删除默认区域近似等面积的测试。
- 保留最小面积验证，并以 `RegionConfig.min_cells_per_region` 为唯一阈值来源。

## 验收标准

- 默认区域划分允许明显的面积差异。
- 任一生成区域的网格数均不小于配置推导出的最小网格数。
- 完整测试套件通过。
