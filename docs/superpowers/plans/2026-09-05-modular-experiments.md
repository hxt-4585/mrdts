+# Modular Methods and Experiment Entrypoints — Historical Decision

完整方案位于 `methods/solutions/<名称>/`，可替换排序、飞行和调度组件位于 `methods/components/`。实验运行、记录、汇总与绘图统一放在 `experiments/`。

当前仅注册 `random` 完整方案；学习方案仍是预留接口。目录与启动约定见 `docs/PROJECT_STRUCTURE.md`。
